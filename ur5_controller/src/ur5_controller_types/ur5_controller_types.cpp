#include <ur5_controller_types/ur5_controller_types.h>

//This cpp is currently only here so the compiler knows which linker to use to create the lib.
//If somebody knows how to only use .h-file to create lib, please remove this and edit CMakeList.txt

//Type definitions are located in the .h-file